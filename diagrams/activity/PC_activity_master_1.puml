@startuml
title Диаграмма деятельности: Работа с Dashboard (Мастер)

start
:Авторизация (Login);
if (Пользователь связан с моделью Master?) then (нет)
    :Сообщение "Нет прав";
    :Редирект на главную;
    stop
else (да)
    :Отображение Dashboard;
    :Просмотр списка "Предстоящие записи";
    
    repeat
        :Выбор конкретной записи;
        :Анализ данных (эскиз, примечания, клиент);
        
        if (Мастер готов принять?) then (да)
            :Выбор статуса "Подтвержден";
        else (нет)
            :Выбор статуса "Отменен";
        endif
        
        :Отправка POST-запроса;
        :Обновление записи в БД;
    repeat while (Есть необработанные записи?)
    
    :Сеанс проведен? (в будущем);
    if (Сеанс завершен успешно) then (да)
        :Смена статуса на "Выполнен";
        :Запись попадает в архив (Past Appointments);
    endif
endif
stop
@enduml